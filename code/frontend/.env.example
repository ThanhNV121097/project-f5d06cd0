# Browser-visible backend origin for client-side API calls.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Server-side backend origin for server components or route handlers.
API_ORIGIN=http://localhost:8080
