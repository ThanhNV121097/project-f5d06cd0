# PostgreSQL connection string injected by runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# Preferred HTTP listen port.
PORT=8080

# Fallback HTTP listen port when PORT is unset.
APP_PORT=8080
