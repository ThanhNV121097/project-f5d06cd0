# project-f5d06cd0
Project type: Full-stack web app — a frontend, a backend HTTP API and a database. It is deliberately NOT a static page: the backend must be a real service exposing REST endpoints, and the data must persist in the database.Customer request:Build a small "Hello World" demo that proves the whole stack works end to end. Keep it minimal — no accounts, n
